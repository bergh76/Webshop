using System.Linq;
using Microsoft.AspNetCore.Mvc;
using Xunit;

// For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860

namespace Webshop.Controllers
{
    public class ArticlesControllerTest

    {
        [Fact]
        public void ArticlesControllerVendroDropdownTest()
        {
            ////Arrange
            //var controller = new ArticlesController();
            //var vendorname = controller.ViewData.ToList();
            ////Act
            //var result = controller.Index(vendorname);
            ////Assert
            //var viewResult = Assert.IsType<ViewResult>(result);
            //var model = Assert.IsAssignableFrom<ArticlesController>(viewResult.ViewData.Model);
            ////Assert.Equal(datetime.Now.ToString(), model.CurrentDateAndTime);
            //Assert.Equal(2, model.Names.Count());
            //Assert.Equal("Andreas", model.Names.ElementAt(0));
        }
    }
}
